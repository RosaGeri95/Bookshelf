<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Bookshelf.Views.Placeholder"
             Title="{Binding Title}">

    <StackLayout Orientation="Vertical">
        <Button Text="List" Command="{Binding ListCommand}"/>
        <ListView ItemsSource="{Binding Shelves}"  RowHeight="80" VerticalOptions="FillAndExpand" BackgroundColor="AntiqueWhite">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Image Source="bookshelf.png" HeightRequest="80" WidthRequest="80"/>
                            <Label Text="{Binding}" HorizontalOptions="FillAndExpand" VerticalOptions="Center"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>